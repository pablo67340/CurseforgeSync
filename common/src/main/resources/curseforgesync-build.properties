# Generated at build time. The sync engine reads this to know which Minecraft version it is
# attached to, because nothing in the game is loaded yet when it runs.
minecraft_version=${minecraft_version}
forge_version=${forge_version}
mod_version=${mod_version}
platform=${platform}
